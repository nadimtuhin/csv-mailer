# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# Use SQLite database file in the prisma directory
DATABASE_URL="file:./prisma/dev.db" # Corrected path to be inside prisma dir
SENDGRID_API_KEY="SG.5z2QTBWLTmaOs2UlN5yXMg.FUt2EEUnvM7"

# Secret key for signing JSON Web Tokens (JWTs)
# IMPORTANT: Replace this with a strong, unique secret in production!
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="YOUR_VERY_SECRET_RANDOM_STRING_REPLACE_ME"
